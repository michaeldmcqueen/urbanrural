source "https://rubygems.org"
gem 'jekyll', '3.9.5'  # last stable Jekyll 3 release
gem 'csv'              # required explicitly in Ruby 3.4+ (removed from stdlib)
gem 'base64'           # required explicitly in Ruby 3.4+ (removed from stdlib)
gem 'ostruct'          # required explicitly in Ruby 3.4+ (removed from stdlib)
gem 'bigdecimal'       # required explicitly in Ruby 3.4+ (removed from stdlib)
gem 'toolkit', '2.3.0'
gem 'singularitygs', '1.4'
gem 'breakpoint', '2.5.0'
gem 'compass', '1.0.3'
group :jekyll_plugins do
  gem 'jekyll-sitemap', '1.4.0'
  gem 'kramdown-parser-gfm'
end
